import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fitstats-new',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
